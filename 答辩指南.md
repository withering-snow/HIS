# HIS 系统答辩指南

> 写给大一生的代码难点解析
> 按文件/层级组织，每个难点都附有"答辩话术"——直接照着说就行

---

## 目录

1. [整体架构：三层架构](#1-整体架构三层架构)
2. [Core 层：基础工具](#2-core-层基础工具)
3. [Entity 层：数据实体](#3-entity-层数据实体)
4. [Ctrl 层：数据控制](#4-ctrl-层数据控制)
5. [Service 层：业务逻辑](#5-service-层业务逻辑)
6. [UI 层：用户界面](#6-ui-层用户界面)
7. [API 层：接口汇总](#7-api-层接口汇总)
8. [答辩常见问题](#8-答辩常见问题)

---

## 1. 整体架构：三层架构

### 1.1 什么是三层架构？

```
┌─────────────────────────────────────┐
│          UI 层（界面展示）            │  ← 用户看到的菜单、输入输出
├─────────────────────────────────────┤
│       Service 层（业务逻辑）          │  ← 核心功能：挂号、看诊、开药...
├─────────────────────────────────────┤
│  Ctrl 层（数据控制） + Entity（数据）  │  ← 存文件、读文件、管理内存
└─────────────────────────────────────┘
```

**为什么用三层架构？**
- 各层各司其职，互不干扰
- 比如要改界面，只改 UI 层，不动业务逻辑
- 比如要改存储方式（文件→数据库），只改 Ctrl 层

### 1.2 数据流向

```
用户操作 → UI层接收输入 → Service层处理 → Ctrl层读写文件
                                              ↓
                                           Entity层（内存中的数据）
```

**答辩话术：**
> "我们采用了经典的三层架构。UI层负责和用户交互，Service层处理业务逻辑，Ctrl层负责数据持久化。这样分层的好处是代码清晰、易于维护，比如将来想从文件存储改成数据库，只需要改Ctrl层，其他代码不用动。"

---

## 2. Core 层：基础工具

### 文件：`include/core/base.h`

#### 2.1 状态码枚举（Status）

```c
typedef enum {
    HIS_OK = 0,
    HIS_ERR_NO_MEM = -1,
    HIS_ERR_NOT_FOUND = -10,
    // ... 更多状态码
} Status;
```

**难点：** 为什么用枚举而不是数字？
- 枚举有名字，比直接写 `return -10` 更容易理解
- 每个函数都返回 Status，统一错误处理

**答辩话术：**
> "我们定义了一个统一的 Status 枚举，所有函数都返回这个类型。这样不管是内存不足、找不到数据、还是余额不够，调用者都能通过统一的返回值知道发生了什么错误。"

#### 2.2 ASSERT 宏（条件编译）

```c
#ifndef NDEBUG
#define ASSERT(condition, message) \
    do { \
        if(!(condition)) { \
            fprintf(stderr, "错误信息..."); \
            abort(); \
        } \
    } while(0)
#else
#define ASSERT(condition, message) ((void)0)
#endif
```

**难点：** 为什么用 `do { ... } while(0)` 包裹？
- 防止宏展开后出现语法错误
- 比如 `if (x) ASSERT(p != NULL, "err"); else ...` 不加 `do-while` 会编译错误

**难点：** 条件编译是什么？
- `NDEBUG` 没定义时（Debug模式），ASSERT 生效
- `NDEBUG` 定义了时（Release模式），ASSERT 变成空操作
- 开发时抓 Bug，发布时零开销

**答辩话术：**
> "ASSERT 是我们的调试利器。Debug模式下，如果条件不满足会打印错误位置并终止程序，帮我们快速定位Bug。Release模式下自动跳过，不影响性能。用 do-while 包裹是为了防止宏展开出问题。"

#### 2.3 safe_malloc

```c
static inline void *safe_malloc(size_t size) {
    void *p = malloc(size);
    if (p == NULL) {
        fprintf(stderr, "!!!!!!!! FATAL: OUT OF MEMORY !!!!!!!!\n");
        fprintf(stderr, "Location: %s (%s:%d)\n", __func__, __FILE__, __LINE__);
    }
    return p;
}
```

**难点：** 为什么不用 malloc 直接分配？
- malloc 失败返回 NULL，如果不检查就直接用，程序会崩溃
- safe_malloc 封装了检查逻辑，失败时打印大红报错

**答辩话术：**
> "标准库的 malloc 如果内存不够会返回 NULL，但很多新手会忘记检查。我们封装了 safe_malloc，自动检查返回值，失败时打印醒目的错误信息，方便调试。"

#### 2.4 cipher_xor_cyclic 密码加密

```c
static inline void cipher_xor_cyclic(char* data, size_t data_len) {
    const char* key = "HIS_XOR";
    for (size_t i = 0; i < data_len; i++) {
        data[i] ^= key[i % strlen(key)];
    }
}
```

**难点：** 异或加密原理
- 同一个数异或两次等于原数：`a ^ b ^ b == a`
- 加密：`明文 ^ 密钥 = 密文`
- 解密：`密文 ^ 密钥 = 明文`
- 所以加密和解密是同一个函数！

**答辩话术：**
> "我们用异或加密来保护密码。原理很简单：明文和密钥做异或得到密文存到文件，读取时再做一次异或就还原成明文。因为异或两次等于原数，所以加密和解密用同一个函数。当然这只是基础混淆，不是真正的安全加密。"

#### 2.5 load_ui_tools 宏

```c
#define load_ui_tools \
static void clear_space() { ... } \
static void get_input_str(...) { ... } \
static long long get_input_long_long(...) { ... } \
static void press_enter() { ... }
```

**难点：** 为什么用宏而不是写在单独的文件里？
- 这些函数用到了 `printf`、`scanf` 等标准 IO
- 如果写在 .c 文件里，每个 UI 文件都要包含
- 用宏展开，每个 UI 文件都能直接使用

**答辩话术：**
> "我们把常用的输入输出函数封装成一个宏，每个UI文件只要写一行 `load_ui_tools` 就能使用这些函数。这样避免了重复写同样的代码。"

---

### 文件：`include/core/list.h` / `src/core/list.c`

#### 2.6 通用链表（List）

```c
typedef struct List* List_T;
```

**难点：** 什么是链表？
- 数组：连续内存，插入删除慢，随机访问快
- 链表：不连续内存，插入删除快，随机访问慢
- 每个节点包含数据和指向下一个节点的指针

**难点：** 为什么用 `void*` 存数据？
- 不知道用户要存什么类型（病人？医生？药品？）
- `void*` 可以指向任何类型，实现"通用"
- 使用时需要强制转型回原类型

**难点：** 链表操作

```c
List_T list = List_new(sizeof(Patient_T));  // 创建链表，指定元素大小
List_push_back(list, &patient);              // 尾部插入
void* p = List_first(list);                  // 获取第一个元素
while (p != NULL) {
    Patient_T pat = *(Patient_T*)p;          // 转型回原类型
    // 处理数据...
    p = List_next(list);                     // 移动到下一个
}
List_free(&list);                            // 释放链表
```

**答辩话术：**
> "我们实现了一个通用链表，可以存储任意类型的数据。通过 `void*` 指针实现通用性，用户只需要在创建时指定元素大小，使用时把 `void*` 转型回原类型即可。链表的好处是插入删除效率高，适合我们这种频繁增删的场景。"

---

## 3. Entity 层：数据实体

### 3.1 实体设计模式

每个实体（病人、医生、药品等）都遵循相同的模式：

```
头文件：声明结构体 + 操作函数
  ↓
源文件：实现操作函数
```

**典型结构：**

```c
// 头文件（如 patient.h）
typedef struct Patient* Patient_T;  // 不透明指针

Patient_T Patient_new(...);          // 创建
void Patient_delete(Patient_T*);     // 销毁
void Patient_update(Patient_T, ...); // 修改
long long Patient_id(Patient_T);     // 获取ID（getter）
```

**难点：** 什么是不透明指针？
- 用户只知道 `Patient_T` 是一个指针
- 但不知道 `struct Patient` 内部有什么成员
- 强制用户只能通过函数操作，不能直接修改内部数据
- 好处：封装性，内部改了不影响外部代码

**答辩话术：**
> "我们用了不透明指针的设计模式。用户只能通过我们提供的函数来操作数据，不能直接访问结构体内部。这样保证了数据的安全性，也方便以后修改内部实现。"

### 3.2 病人实体（Patient）

**难点：** 年龄计算

```c
int Patient_age(Patient_T patient) {
    time_t now = time(NULL);
    struct tm* now_tm = localtime(&now);
    int birth_year = patient->birth_year;
    int now_year = now_tm->tm_year + 1900;
    int age = now_year - birth_year;
    // 如果今年生日还没过，减一岁
    if (now_tm->tm_mon + 1 < patient->birth_month ||
        (now_tm->tm_mon + 1 == patient->birth_month &&
         now_tm->tm_mday < patient->birth_day)) {
        age--;
    }
    return age;
}
```

**难点：** 为什么年龄不直接存？
- 年龄每年都在变，存了也要更新
- 存出生日期，每次用的时候计算，永远准确

**答辩话术：**
> "病人的年龄是动态计算的，不是直接存一个数字。我们存的是出生日期，每次获取年龄时根据当前日期计算。这样年龄会自动更新，不需要每年手动改。"

### 3.3 药品实体（Medicine）

**难点：** 批次管理

```c
typedef struct {
    long long id;
    long long buy_price;    // 进价
    long long expire_ts;    // 过期时间
    int remain;             // 剩余数量
    BatchStatus status;     // 状态：可用/用尽/弃置
    char batch_no[32];      // 批次号
} MedicineBatch;
```

**难点：** 为什么药品要有批次？
- 同一药品不同批次进价不同
- 过期时间不同，需要先进先出
- 某个批次出问题可以单独报废

**答辩话术：**
> "药品管理需要批次概念。同一药品不同时间进货，进价和过期时间都不一样。我们按批次管理，开药时优先使用先过期的批次（先进先出），某个批次有问题可以单独报废，不影响其他批次。"

### 3.4 病房实体（Ward）

**难点：** 床位管理

```c
typedef struct {
    int bed_label;       // 床号
    BedStatus status;    // 状态：空闲/有人/维修
    long long pat_id;    // 入住病人ID
    long long start_ts;  // 入住时间
} Bed_T;
```

**难点：** 为什么床位是病房内部管理的？
- 床位属于病房，不能独立存在
- 病房创建时自动生成床位
- 病房删除时床位也一起删除

**答辩话术：**
> "床位是病房的一部分，病房创建时自动生成指定数量的床位。每个床位记录状态（空闲/有人/维修）、入住病人和入住时间。这样方便管理病房的入住情况。"

### 3.5 记录实体（Record）

**难点：** 联合体（union）存储不同类型数据

```c
typedef struct Record* Record_T;

typedef struct {
    DataRegistration registration;  // 挂号数据
    DataConsultation consultation;  // 看诊数据
    DataExamination examination;    // 检查数据
    DataPrescription prescription;  // 开药数据
    DataAdmission admission;        // 入院数据
    DataDischarge discharge;        // 出院数据
} RecordDetail;

struct Record {
    long long id;
    long long actor_id;     // 操作者ID（病人/医生）
    long long target_id;    // 目标ID
    RecordType type;        // 记录类型
    long long cost;         // 费用
    long long time_stamp;   // 时间戳
    bool is_invalid;        // 是否无效
    RecordDetail detail;    // 具体数据
};
```

**难点：** 为什么不用 union 而用 struct？
- union 所有成员共享内存，节省空间
- 但使用起来容易出错，需要记住当前是哪种类型
- 用 struct 虽然浪费一点空间，但更安全、更易用

**答辩话术：**
> "记录有多种类型（挂号、看诊、检查、开药、入院、出院），每种类型的数据结构不同。虽然用 union 更省空间，但为了代码安全和可读性，我们用了 struct，每种类型的数据都保留。反正现代电脑不缺这点内存。"

---

## 4. Ctrl 层：数据控制

### 文件：`src/ctrl/io_ctrl.c`

#### 4.1 文件读写

**难点：** 为什么用 `|` 作为分隔符？
- CSV 用逗号，但数据里可能有逗号
- `|` 在正常数据中几乎不会出现
- 用 `fscanf` 的 `%[^|]` 格式读取

**难点：** 文件格式

```
病人文件（Patient.txt）：
1|赵大宝|1|1995-03-10|320101199503101234|13800138001

挂号记录（Record.txt）：
1|1|1|REGISTRATION|5000|1716000000|0|1|20260519|1|APPOINTMENT
```

**答辩话术：**
> "我们用文本文件存储数据，用 `|` 作为字段分隔符。选择 `|` 是因为它几乎不会出现在正常数据中。每行一条记录，方便调试时直接查看和修改。"

#### 4.2 Io_load_record 的 Bug 修复

**难点：** `%[^|]` 的陷阱

```c
// 错误写法（会导致崩溃）
fscanf(file, "%[^|]|%[^|]|...", buf1, buf2, ...);

// 正确写法
fscanf(file, "%[^|\n]|%[^|\n]|...", buf1, buf2, ...);
```

**为什么？**
- `%[^|]` 遇到 `|` 停止，但不会处理换行符 `\n`
- 如果某行末尾没有 `|`，`%[^|]` 会一直读到文件末尾
- 加上 `\n` 后，`%[^|\n]` 遇到 `|` 或 `\n` 都会停止

**答辩话术：**
> "这里有个坑。`%[^|]` 只会在遇到 `|` 时停止，但如果某行末尾没有 `|`，它会一直读到文件末尾，导致后续解析全部错乱。我们改成 `%[^|\n]`，遇到 `|` 或换行都会停止，这样就不会出问题了。"

### 文件：`src/ctrl/rel_ctrl.c`

#### 4.3 关系控制（排队系统）

**难点：** 排队队列管理

```c
// 医生叫号
Status Rel_call_reg(long long doc_id) {
    // 找到该医生队列中第一个 WAITING 状态的病人
    // 将其状态改为 IN_CONSULTATION
    // 返回该病人ID
}

// 病人签到
Status Rel_patient_checkin(long long doc_id, long long pat_id) {
    // 找到该医生队列中该病人的预约记录
    // 将状态从 APPOINTMENT 改为 WAITING
    // 分配排队号
}
```

**答辩话术：**
> "排队系统是核心功能。病人先预约挂号（APPOINTMENT），然后签到（变为WAITING），医生叫号（变为IN_CONSULTATION），看诊结束（变为COMPLETED）。每个状态转换都有严格的逻辑控制。"

---

## 5. Service 层：业务逻辑

### 文件：`src/service/doctor_serv.c`

#### 5.1 医生看诊流程

```c
Status Serv_doc_consult(const char* diagnosis, const char* advice) {
    // 1. 获取当前登录医生
    long long doc_id = Serv_account_cur_id();
    // 2. 获取当前看诊病人
    long long pat_id = Serv_doc_view_reg();
    // 3. 创建看诊记录
    Record_T record = Record_new(...);
    // 4. 扣费（挂号费在签到时已扣）
    // 5. 保存记录
    return HIS_OK;
}
```

**难点：** 为什么看诊后不自动释放病人？
- 之前的设计是看诊后自动释放，但医生可能还要开药、检查
- 改为手动释放（菜单12），医生可以连续操作

**答辩话术：**
> "我们特意把看诊和释放病人分开了。看诊后医生可能还需要开药、开检查，如果自动释放病人，这些操作就无法进行了。所以医生完成所有操作后，手动选择'结束看诊'来释放病人。"

### 文件：`src/service/patient_serv.c`

#### 5.2 预约挂号

```c
Status Serv_patient_register(long long doc_id, int target_date, int time_frame) {
    // 1. 检查该时段是否已满（每个时段最多20人）
    // 2. 创建挂号记录
    // 3. 状态设为 APPOINTMENT（待签到）
    // 4. 分配排队号
}
```

**难点：** 时间段管理
- 一天分17个时段（0=急诊，1~8=上午，9~16=下午）
- 每个时段30分钟
- 每个时段最多20人

**答辩话术：**
> "我们把一天分成17个时段，每个时段30分钟，每个时段最多容纳20个病人。预约时选择科室→医生→日期→时段，如果某个时段已满，系统会提示选择其他时段。"

### 文件：`src/service/serv_base/helper_serv.c`

#### 5.3 辅助函数

```c
// 通过ID查找任意类型的实体
void* Serv_helper_finder(long long id, EntityType type) {
    switch(type) {
        case TYPE_PATIENT:  // 在病人列表中查找
        case TYPE_DOCTOR:   // 在医生列表中查找
        case TYPE_MEDICINE: // 在药品列表中查找
        // ...
    }
}

// 通过ID获取名称
const char* Serv_helper_id_to_name(long long id, EntityType type) {
    void* entity = Serv_helper_finder(id, type);
    switch(type) {
        case TYPE_PATIENT: return Patient_name((Patient_T)entity);
        case TYPE_DOCTOR:  return Doctor_name((Doctor_T)entity);
        // ...
    }
}
```

**答辩话术：**
> "我们写了一些通用辅助函数。`Serv_helper_finder` 可以根据ID和类型查找任意实体，`Serv_helper_id_to_name` 可以根据ID获取名称。这样避免了在每个地方都写重复的查找代码。"

---

## 6. UI 层：用户界面

### 文件：`src/ui/main_ui.c`

#### 6.1 登录逻辑

```c
Status UI_main_menu() {
    // 1. 显示登录界面
    // 2. 输入账号密码
    // 3. 根据角色进入不同菜单
    //    管理员 → root_ui.c
    //    病人   → pat_ui.c
    //    医生   → doc_ui.c
}
```

**难点：** 角色路由
- 不同角色看到不同的菜单
- 管理员管理全系统
- 病人只能看自己的信息
- 医生处理诊疗业务

**答辩话术：**
> "登录后系统根据角色自动跳转到不同的界面。管理员看到的是管理菜单，病人看到的是个人服务菜单，医生看到的是诊疗工作站。每个角色只能做自己权限范围内的事。"

### 文件：`src/ui/doc_ui.c`

#### 6.2 医生工作站

**难点：** 菜单循环

```c
while (UI_doc_should_continue) {
    // 显示菜单
    // 获取用户选择
    // 执行对应操作
    // 按回车继续
}
```

**难点：** 当前看诊病人状态

```c
long long cur_pat = Serv_doc_view_reg();
if (cur_pat != INVALID_ID) {
    printf("当前看诊病人: %s (ID: %lld)\n", name, cur_pat);
} else {
    printf("当前无看诊病人\n");
}
```

**答辩话术：**
> "医生工作站是一个循环菜单，每次循环都会显示当前看诊的病人。医生可以叫号、看诊、开药、检查、办理入院出院等。所有操作都在这个循环中完成，直到医生选择退出。"

### 文件：`src/ui/pat_ui.c`

#### 6.3 预约挂号界面

**难点：** 动态过滤已过时段

```c
if (target_date == today) {
    int cur_total_min = cur_hour * 60 + cur_min;
    for (int i = 1; i <= 16; i++) {
        int start_hour = 8 + (i - 1) / 2;
        int start_min = (i - 1) % 2 * 30;
        if (start_hour * 60 + start_min > cur_total_min) {
            min_tf = i;
            break;
        }
    }
}
```

**答辩话术：**
> "预约挂号时，如果选的是今天，系统会自动计算当前时间，把已经过去的时间段隐藏掉。比如现在是上午10点，那8点和8点半的时段就不会显示，病人只能选还没开始的时段。"

---

## 7. API 层：接口汇总

### 文件：`include/api/`

API 层是给外部程序调用的接口，把所有功能汇总在一起。

```c
// 病人端API
Status API_patient_register(...);  // 预约挂号
Status API_patient_checkin(...);   // 签到
Status API_patient_records(...);   // 查询记录

// 医生端API
Status API_doc_consult(...);       // 看诊
Status API_doc_prescribe(...);     // 开药
Status API_doc_admission(...);     // 入院

// 管理员API
Status API_root_add_doctor(...);   // 新增医生
Status API_root_add_medicine(...); // 新增药品
```

**答辩话术：**
> "API层是系统的对外接口，把所有功能封装成统一的函数。虽然目前只有命令行界面，但将来如果想做网页版或手机App，直接调用API层就可以了，不需要重新实现业务逻辑。"

---

## 8. 答辩常见问题

### Q1: 为什么用 C 语言而不是 Java/Python？

**答：**
> "C语言更接近底层，能让我们更好地理解内存管理、指针、数据结构等计算机基础概念。作为大一学生，用C语言做项目能巩固课堂上学到的知识。而且C语言性能好，适合这种需要频繁读写文件的系统。"

### Q2: 数据存在文件里，如果程序崩溃了怎么办？

**答：**
> "我们的数据是实时保存的，每次修改都会立即写回文件。如果程序崩溃，最多丢失当前操作的数据，之前的数据都已经保存好了。当然，如果将来要商用，应该改用数据库（如MySQL、SQLite），它们有事务机制保证数据完整性。"

### Q3: 密码安全吗？

**答：**
> "我们用了异或加密来保护密码，虽然不是最安全的加密方式（比如和真正的AES加密比差很远），但对于课程项目来说已经足够了。密码在文件里是密文存储的，即使有人打开了文件也看不到原始密码。实际项目中应该用哈希加盐的方式存储密码。"

### Q4: 链表和数组有什么区别？为什么用链表？

**答：**
> "数组在内存中是连续的，访问快（O(1)），但插入删除慢（O(n)）。链表在内存中不连续，插入删除快（O(1)），但访问慢（O(n)）。我们的系统需要频繁增删数据（比如挂号、排队），所以链表更合适。"

### Q5: 你们遇到了哪些坑？

**答：**
> "最大的坑是文件解析。`fscanf` 的 `%[^|]` 格式在遇到换行符时不会停止，导致解析错乱、程序崩溃。我们花了很长时间才找到原因，最后改成 `%[^|\n]` 解决了。
>
> 另一个坑是看诊后自动释放病人，导致医生无法继续开药检查。我们改成了手动释放，让医生完成所有操作后再结束看诊。
>
> 还有一个是病人签到功能缺失，导致医生永远叫不到号。我们补上了签到功能，病人预约后需要签到才能进入排队队列。"

### Q6: 你们的分工是怎样的？

**答：**
> "我作为组长负责系统整体架构设计、核心代码编写和项目整合，具体包括：系统框架总体分析与设计，基本工具与数据结构的实现，服务层的设计与实现，操作界面层实现，代码规范与审查，项目测试与修复，生成测试数据，撰写总结报告。
>
> 组员A参与了操作界面层的设计讨论，输出了界面原型方案，并负责项目测试工作，编写测试用例、执行功能测试、记录Bug。
>
> 组员B参与了实体层的设计与编码实现，负责病人、医生、药品等核心实体的数据结构设计，并协助排查和修复了程序中的部分Bug。
>
> 组员C参与了数据持久化模块的设计，负责文件读写格式的制定与实现，以及日志系统的设计与编码。"

### Q7: 项目有多少代码量？

**答：**
> "项目共有约 30 个源文件，包括头文件和实现文件，总代码量约 5000-6000 行。其中核心业务逻辑约 2000 行，界面代码约 1500 行，数据管理约 1000 行，基础工具约 500 行。"

### Q8: 如果让你改进，你会加什么功能？

**答：**
> "如果时间允许，我们会：
> 1. 改用数据库存储（SQLite），解决文件并发访问问题
> 2. 增加图形界面（Qt 或 Web），提升用户体验
> 3. 增加数据统计功能（如月度收入报表、药品消耗分析）
> 4. 增加权限管理（不同级别的医生有不同的操作权限）
> 5. 增加日志审计功能（记录所有操作，方便追溯）"

---

## 9. 加分亮点（答辩时主动展示）

以下亮点是你们项目的加分项，答辩时**主动提出来**会让老师眼前一亮：

### 亮点1：三层架构设计
> "我们采用了经典的三层架构（UI-Service-Ctrl），各层职责清晰。UI层只负责界面交互，Service层处理业务逻辑，Ctrl层管理数据持久化。这样设计的好处是：如果要改存储方式（比如从文件改成数据库），只需要改Ctrl层，其他代码完全不用动。"

### 亮点2：通用链表实现
> "我们自己实现了一个通用链表，可以存储任意类型的数据。用 `void*` 指针实现通用性，用户只需要在创建时指定元素大小。这比直接用数组灵活得多，插入删除效率高，适合我们这种频繁增删的场景。"

### 亮点3：不透明指针封装
> "所有实体都用了不透明指针设计，用户只能通过我们提供的函数操作数据，不能直接访问结构体内部。这体现了面向对象的封装思想，虽然是C语言，但我们用这种方式实现了信息隐藏。"

### 亮点4：条件编译的 ASSERT
> "我们的 ASSERT 宏支持条件编译。Debug模式下会检查条件并打印错误位置，Release模式下自动跳过，零开销。这体现了工程化思维——开发时方便调试，发布时不影响性能。"

### 亮点5：动态年龄计算
> "病人的年龄不是直接存储的，而是根据出生日期动态计算。这样年龄会自动更新，不需要每年手动修改。虽然只是一个小细节，但体现了'数据应该只存一份'的设计原则。"

### 亮点6：药品批次管理（先进先出）
> "药品管理实现了批次级别的精细控制。同一药品不同批次进价不同、过期时间不同，开药时优先使用先过期的批次（FIFO，先进先出），某个批次有问题可以单独报废。这已经接近真实药房的库存管理系统了。"

### 亮点7：预约时段动态过滤
> "预约挂号时，如果选的是今天，系统会自动计算当前时间，把已经过去的时间段隐藏掉。这个细节虽然小，但用户体验很好，病人不会选到已经过期的时段。"

### 亮点8：排队状态机
> "挂号记录有完整的状态流转：APPOINTMENT（预约）→ WAITING（签到候诊）→ IN_CONSULTATION（看诊中）→ COMPLETED（完成）。每个状态转换都有严格的逻辑控制，系统启动时还会自动清理过期未签到的预约。"

### 亮点9：统一的错误处理
> "所有函数都返回统一的 Status 枚举，调用者可以通过返回值知道具体发生了什么错误（内存不足、找不到数据、余额不够等）。这比返回 -1 或 0 要清晰得多。"

### 亮点10：日志系统
> "我们实现了日志系统，所有关键操作（登录、修改密码、充值、看诊等）都会记录到日志文件，包含时间、操作者、操作内容。这在实际系统中是必备的审计功能。"

### 亮点11：safe_malloc 大红报错
> "我们封装了 safe_malloc，内存分配失败时会打印大红底报错信息，包含文件名、行号和申请大小。这比直接崩溃或者静默返回 NULL 要友好得多。"

### 亮点12：密码异或加密
> "虽然只是简单的异或加密，但我们实现了密码的密文存储。文件里存的是加密后的密码，即使有人打开了数据文件也看不到原始密码。这体现了最基本的安全意识。"

---

> 最后提醒：答辩时不要照着念，要理解后用自己的话说。遇到不会的问题，可以说"这个问题我们确实考虑过，目前的做法是...，但如果有更好的方案我们也愿意学习改进"。
